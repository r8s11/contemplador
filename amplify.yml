version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - yarn install --immutable
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
  cache:
    paths:
      - .yarn/cache/**/*
      - node_modules/**/*
